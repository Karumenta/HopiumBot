services:
  hopiumbot:
    build: .
    container_name: hopiumbot
    env_file: .env
    restart: unless-stopped
    networks:
      - hopiumbot-net

networks:
  hopiumbot-net:
    name: hopiumbot-net
